[package]
name = "vulcanum-host-server"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
tracing = "0.1"
vulcanum-shared = { path = "../shared" }
