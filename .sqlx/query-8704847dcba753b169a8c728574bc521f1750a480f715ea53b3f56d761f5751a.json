{
  "db_name": "PostgreSQL",
  "query": "WITH candidates AS (\n                SELECT id, worker_id\n                FROM work_runs\n                WHERE status = $1\n                AND updated_at < NOW() - INTERVAL '1 second' * $2\n                AND finish_blocked_reason IS NULL\n            ),\n            reset_runs AS (\n                UPDATE work_runs wr\n                SET status = 'pending'::work_run_status, worker_id = NULL\n                FROM candidates c\n                WHERE wr.id = c.id\n                RETURNING c.worker_id\n            ),\n            affected_workers AS (\n                SELECT worker_id, COUNT(*)::int AS reset_count\n                FROM reset_runs\n                WHERE worker_id IS NOT NULL\n                GROUP BY worker_id\n            ),\n            updated_workers AS (\n                UPDATE workers w\n                SET active_jobs = GREATEST(w.active_jobs - affected_workers.reset_count, 0),\n                    status = CASE\n                        WHEN GREATEST(w.active_jobs - affected_workers.reset_count, 0) = 0\n                         AND w.status = 'busy'::worker_status\n                        THEN 'idle'::worker_status\n                        ELSE w.status\n                    END\n                FROM affected_workers\n                WHERE w.id = affected_workers.worker_id\n                RETURNING w.id\n            )\n            SELECT COUNT(*)::bigint AS \"reset_count!\"\n            FROM reset_runs\n            CROSS JOIN (SELECT COUNT(*) FROM updated_workers) AS updated_worker_count",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "reset_count!",
        "type_info": "Int8"
      }
    ],
    "parameters": {
      "Left": [
        {
          "Custom": {
            "name": "work_run_status",
            "kind": {
              "Enum": [
                "pending",
                "dispatched",
                "running",
                "completed",
                "failed",
                "stalled"
              ]
            }
          }
        },
        "Float8"
      ]
    },
    "nullable": [
      null
    ]
  },
  "hash": "8704847dcba753b169a8c728574bc521f1750a480f715ea53b3f56d761f5751a"
}
