{
  "private": true,
  "name": "@repo/server",
  "scripts": {
    "build": "cargo build -p vulcanum-server",
    "lint": "cargo fmt --check && cargo clippy --all-targets -p vulcanum-server -- -D warnings",
    "test": "cargo test -p vulcanum-server",
    "format": "cargo fmt"
  }
}
