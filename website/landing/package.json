{
  "name": "@repo/landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "biome check --error-on-warnings .",
    "format": "biome check --error-on-warnings --write --unsafe .",
    "validate": "pnpm lint && pnpm build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "vite": "^7.0.4"
  }
}
